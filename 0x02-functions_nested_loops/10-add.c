#include "main.h"

/**
 * add - Adding two param together
 *
 * @x: first param provided
 * @y: second param position
 *
 * Return: adds the two param together
 */
int add(int x, int y)
{
	return ((x + y));
}
